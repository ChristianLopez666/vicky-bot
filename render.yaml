services:
  - type: web
    name: vicky-fase1
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app --bind 0.0.0.0:$PORT
    envVars:
      - key: LOG_LEVEL
        value: INFO
